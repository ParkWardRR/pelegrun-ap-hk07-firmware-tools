// Package hood — the safety gate that keeps the raptor calm: verify the u-boot env is COMPLETE before any write, append-only fw_setenv (never erase/rebuild), enforce dual-A/B-slot. PLANNED (dev phase 3).
package hood
