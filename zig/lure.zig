//! lure — minimal TFTP/BOOTP responder for u-boot deep-brick recovery.
//! PLANNED (dev phase 5). Stub only.
const std = @import("std");

pub fn main() !void {
    std.debug.print("lure: TFTP/BOOTP recovery responder — planned (dev phase 5)\n", .{});
}
